# sbl_68k

# A minimal bootsector which depacks and starts 68k code to any RAM address >= $6000.

Create a binary and then build a minimal header like in header.js

Now concatenate sbl.XXX and the binary => Ready to launch ROM.
